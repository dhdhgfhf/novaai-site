window.__NOVA_HTML.push(" لـ','اعمل لي صورة','اعمللي صورة لـ','اعمللي صورة','اعمل صورة لـ','اعمل صورة',\n    'اصنع لي صورة لـ','اصنع لي صورة','اصنعلي صورة لـ','اصنعلي صورة','اصنع صورة لـ','اصنع صورة',\n    'ولد لي صورة لـ','ولد لي صورة','ولد صورة لـ','ولد صورة',\n    'انشئ لي صورة لـ','انشئ لي صورة','انشئ صورة لـ','انشئ صورة',\n    'صمم لي صورة لـ','صمم لي صورة','صمم صورة لـ','صمم صورة',\n    'كون لي صورة لـ','كون لي صورة','كون صورة لـ','كون صورة',\n    'اعطني صورة لـ','اعطني صورة','اديني صورة لـ','اديني صورة',\n    'وريني صورة لـ','وريني صورة','اريني صورة لـ','اريني صورة',\n    'تصميم صورة لـ','تصميم صورة','توليد صورة لـ','توليد صورة','تصور لي',\n    'صورة فيها','صورة بتاع','صورة عن','صورة لـ','صورة ل','صورة',\n    'generate an image of','generate image of','generate a picture of',\n    'generate an image','generate image','generate a picture',\n    'create an image of','create image of','create a picture of',\n    'create an image','create image','create a picture',\n    'make an image of','make image of','make a picture of',\n    'make an image','make a picture',\n    'image of','picture of','photo of',\n    'draw me','draw a','draw an','draw ',\n    'sketch me','sketch a','paint me','paint a'\n  ];\n  triggers.sort((a,b)=>b.length-a.length);\n  for(const trig of triggers){\n    const normTrig=normalizeArabic(trig.toLowerCase());\n    const idx=lower.indexOf(normTrig);\n    if(idx!==-1){\n      // slice from the ORIGINAL (non-normalized) text at the matched position,\n      // using the normalized index since lengths stay aligned char-for-char\n      let after=t.slice(idx+trig.length).trim();\n      after=after.replace(/^لـ?\\s*/,''); // strip leftover ل / لـ prefix\n      if(after.length>1) return after;\n    }\n  }\n  // No real subject found after the trigger phrase (e.g. just \"اعمل لي صورة\" alone) —\n  // return empty string so the caller knows to ask a clarifying question\n  // instead of generating a blank/nonsense image from the trigger phrase itself.\n  return \"\";\n}\n\nfunction withTimeout(promise, ms, msg){\n  return new Promise((resolve,reject)=>{\n    let done=false;\n    const t=setTimeout(()=>{ if(!done){done=true;reject(new Error(msg||\"timeout\"));} },ms);\n    promise.then(v=>{ if(!done){done=true;clearTimeout(t);resolve(v);} })\n           .catch(e=>{ if(!done){done=true;clearTimeout(t);reject(e);} });\n  });\n}\n\n/* ── Translate + enhance the prompt: auto-detects style, defaults to photorealism ── */\nasync function enhanceImagePrompt(rawPrompt){\n  const sys = `أنت متخصص في صياغة أوصاف احترافية لنماذج توليد الصور (text-to-image) بأعلى جودة ممكنة.\nسيصلك وصف من المستخدم بأي لغة كانت (عربية، إنجليزية، فرنسية، أو أي لغة أخرى) — افهمه بدقة أياً كانت لغته.\nمهمتك بالترتيب:\n1) ترجم الوصف للإنجليزية بدقة تامة أياً كانت لغة المصدر، محافظاً على نفس المعنى والعناصر بالضبط.\n2) إن ذكر المستخدم أسلوباً فنياً معيناً (anime, cartoon, 3D render, oil painting, watercolor, sketch, pixel art, comic, إلخ) اجعل هذا الأسلوب واضحاً في الوصف الإنجليزي، مع تفاصيل جودة تناسبه.\n3) إن لم يُذكر أي أسلوب، اجعل الصورة واقعية للغاية — كأنها صورة فوتوغرافية حقيقية التقطتها كاميرا احترافية، وليست رسمة أو تصميماً. أضف تفاصيل دقيقة مثل: \"photorealistic, hyperrealistic, shot on Canon EOS R5, 85mm lens, natural lighting, realistic skin texture and pores, accurate anatomy and proportions, sharp focus, professional photography, cinematic lighting, 8k UHD, RAW photo\".\n4) أضف وصفاً بصرياً غنياً ودقيقاً (الإضاءة، زاوية التصوير، التكوين، الألوان، الجو العام، الخلفية) في 2-3 جمل.\n5) لا تُخل بأي عنصر ذكره المستخدم — العدد، اللون، الوضعية، البيئة يجب أن تُذكر بدقة تطابق الوصف الأصلي تماماً.\nرد فقط بالوصف الإنجليزي النهائي، بدون علامات اقتباس أو أي شرح إضافي.`;\n\n  // Try Anthropic first (works inside Claude.ai, no key needed)\n  // Haiku is plenty capable for this quick translate+enhance task, and much faster\n  try{\n    const r=await fetch(\"https://api.anthropic.com/v1/messages\",{\n      method:\"POST\",\n      headers:{\"content-type\":\"application/json\"},\n      body:JSON.stringify({\n        model:\"claude-haiku-4-5-20251001\",\n        max_tokens:180,\n        system:sys,\n        messages:[{role:\"user\",content:rawPrompt}]\n      })\n    });\n    if(r.ok){\n      const d=await r.json();\n      const txt=(d.content||[]).map(b=>b.type===\"text\"?b.text:\"\").join(\"\").trim();\n      if(txt) return txt;\n    }\n  }catch(e){}\n\n  // Fallback: Groq (if user added a key)\n  if(apiKey){\n    try{\n      const r2=await fetch(\"https://api.groq.com/openai/v1/chat/completions\",{\n        method:\"POST\",\n        headers:{\"Content-Type\":\"application/json\",\"Authorization\":\"Bearer \"+apiKey},\n        body:JSON.stringify({\n          model:\"llama-3.1-8b-instant\",\n          max_tokens:180,\n          messages:[{role:\"system\",content:sys},{role:\"user\",content:rawPrompt}]\n        })\n      });\n      if(r2.ok){\n        const d2=await r2.json();\n        const txt=d2.choices?.[0]?.message?.content?.trim();\n        if(txt) return txt;\n      }\n    }catch(e){}\n  }\n\n  return rawPrompt;\n}\n\n/* ── Image generation via Pollinations.ai — pure URL, no auth, no redirect ── */\n/* ── Pollinations NEW API (2026) — base: gen.pollinations.ai\n   Old image.pollinations.ai/prompt/ endpoint is retired.\n   Image generation now requires a key (pk_ publishable key is safe for browsers). ── */\n/* ── Pollinations image generation ─────────────────────────────────────\n   image.pollinations.ai/prompt/{prompt} — the ORIGINAL, still-free,\n   no-signup, no-key endpoint (confirmed working as of July 2026 per\n   multiple independent sources — anonymous requests are just rate-limited\n   to roughly one every 15 seconds, no account required).\n   The newer gen.pollinations.ai/image/{prompt} endpoint requires a Bearer\n   key and offers extra \"Pollen\"-tier models — used automatically as a\n   bonus/first attempt ONLY if the user has optionally added a key,\n   otherwise skipped entirely so nothing blocks free use. ── */\n\nfunction buildLegacyUrl(prompt, model, seed){\n  const boosted = prompt + \", highly detailed, ultra sharp focus, professional quality, 8k\";\n  const full = encodeURIComponent(boosted);\n  return `https://image.pollinations.ai/prompt/${full}?width=1440&height=1440&nologo=true&model=${model}&seed=${seed}`;\n}\n\nfunction preloadImage(url, timeoutMs){\n  return new Promise((resolve,reject)=>{\n    let done=false;\n    const img=new Image();\n    img.onload=()=>{ if(!done){done=true;resolve();} };\n    img.onerror=()=>{ if(!done){done=true;reject(new Error(\"تعذر تحميل الصورة\"));} };\n    img.src=url;\n    setTimeout(()=>{ if(!done){done=true;reject(new Error(\"انتهت المهلة\"));} },timeoutMs||25000);\n  });\n}\n\nfunction buildKeyedUrl(prompt, model, seed){\n  const boosted = prompt + \", highly detailed, ultra sharp focus, professional quality, 8k\";\n  const full = encodeURIComponent(boosted);\n  return `https://gen.pollinations.ai/image/${full}?width=1440&height=1440&seed=${seed}${model?\"&model=\"+encodeURIComponent(model):\"\"}`;\n}\n\nasync function fetchKeyedImage(prompt, model, seed, timeoutMs){\n  const url = buildKeyedUrl(prompt, model, seed);\n  const ctrl = typeof AbortC\");\n