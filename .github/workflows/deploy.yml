name: Legacy NOVA AI deployment (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Use deploy-v2.yml for NOVA AI Pages deployment."
