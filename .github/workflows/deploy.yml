name: Deploy NOVA AI to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build raw NOVA AI 3.6 page
        run: node scripts/build-original-raw.mjs

      - name: Verify raw original page
        run: |
          test -s nova-ai-original/index.html
          grep -q 'initApp' nova-ai-original/index.html
          if grep -qE 'part-[123]\.js|Failed to fetch|fetch\(|DecompressionStream|const input=' nova-ai-original/index.html; then
            echo "Original page is still a loader instead of the raw app"
            exit 1
          fi
          echo "Raw original page size: $(wc -c < nova-ai-original/index.html) bytes"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
